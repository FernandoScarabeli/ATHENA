# Optional local settings for deploy/deploy.sh. Copy to .env.deploy if needed.
# Both paths are local-only and ignored by Git.
DEPLOY_BACKUP_DIR=deploy/backups
DEPLOY_RESET_HASH_FILE=deploy/.reset-db.sha256
