import { Module } from '@nestjs/common'; import { JwtModule } from '@nestjs/jwt'; import { AuthController } from './auth.controller'; import { AuthService } from './auth.service'; import { JwtCookieGuard } from '../common/auth';
@Module({ imports: [JwtModule.register({})], providers: [AuthService, JwtCookieGuard], controllers: [AuthController], exports: [JwtCookieGuard, JwtModule] }) export class AuthModule {}
