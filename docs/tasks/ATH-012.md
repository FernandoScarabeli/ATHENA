# ATH-012 — Revisão visual e operação

**Prioridade:** P2.3. **Dependências:** ATH-011. **Estado:** Em andamento (auditoria parcial).

## Evidência atual — 12/09/2026

O navegador passou a estar disponível e permitiu capturar desktop, Mapa, Importações, mobile 390px, sequência básica de Tab e console. Com os dados posteriormente adicionados ao workspace, foram revisados também grafo/relação, editor TipTap, histórico/diff e drawer de comentários, inclusive em mobile. A correção de overflow da navegação mobile foi aplicada e validada. O relatório está em `ATH-012_VISUAL_AUDIT.md`; ainda faltam fluxos mutáveis/RBAC, sugestões/candidatos reais, estados de falha, foco/contraste completos e verificação operacional controlada.

A verificação operacional já feita permanece parcial: Compose/configuração, scripts, schema, build, lint e testes foram validados; health/readiness da stack existente foram consultados apenas em leitura. Boot limpo, backup/restore e a revisão visual/a11y continuam exigindo ambiente controlado.

## Problema e resultado

Validar o produto executado em desktop/mobile e a operação documentada. Corrigir defeitos reproduzidos de layout, foco, teclado, contraste, console, inicialização Docker e migrations.

## Aceite

- Fluxos Pastas → US → Grafo, editor, comentários, templates e Canceladas são utilizáveis em viewport desktop/mobile.
- Ações de teclado e estados de foco são visíveis; console sem erro nos fluxos testados.
- Compose sobe em ambiente limpo; health/ready, backup/restore e demo são verificados quando dependências externas estiverem disponíveis. Falta de Tailscale/Netlify é registrada, não simulada como sucesso.
- `test`, `build`, `lint` e evidências visuais/operacionais registrados.

## Evidências operacionais (2026-09-12)

- `docker compose -f docker-compose.yml config`: passou.
- `docker compose -f docker-compose.yml -f docker-compose.dev.yml config`: passou.
- O overlay demo também passou a interpolação com `NETLIFY_SITE_ORIGIN=https://demo.invalid`; sem essa variável, o Compose falha de propósito com a mensagem de configuração obrigatória.
- `corepack pnpm build`, `corepack pnpm test` e `corepack pnpm lint`: passaram (API, frontend e pacote compartilhado).
- O healthcheck da API usa `/api/ready`, que valida HTTP e a consulta `SELECT 1` no PostgreSQL; `/api/health` continua disponível como liveness.
- Não foram executados `up`, backup, restore, reset ou migrations contra um banco real durante esta checagem. O coordenador confirmou de forma somente leitura que a stack já em execução responde `GET /api/health` com `{"status":"ok"}` e `GET /api/ready` com `{"status":"ready"}`; isso não substitui o teste de subida limpa nem backup/restore.
- A revisão visual posterior está registrada em `ATH-012_VISUAL_AUDIT.md`. Ela confirmou estados vazios e, após inserção manual do usuário, mapa/relação, editor, histórico e comentários; um overflow mobile foi corrigido. A auditoria ainda não cobre foco/contraste completo, fluxos mutáveis, sugestões/candidatos reais nem falha/retry. Os warnings repetidos do `linkifyjs` foram corrigidos e não se repetiram após reload; o console não teve erros.
